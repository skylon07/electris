{
    "fileTypes": [
        "gitignore"
    ],
    "scopeName": "electris.text.gitignore",
    "patterns": [
        { "include": "#comment" },
        { "include": "#operators" },
        { "include": "#directory-punctuation" },
        { "include": "#file-word" }
    ],
    "repository": {
        "comment": {
            "name": "electris.gitignore.comment",
            "match": "^#.*"
        },

        "file-word": {
            "name": "electris.gitignore.file",
            "match": "[_.\\-a-zA-Z0-9]+"
        },

        "directory-punctuation": {
            "name": "electris.gitignore.punctuation",
            "match": "[~/]"
        },

        "operators": {
            "name": "electris.gitignore.operator",
            "match": "^!|\\*"
        }
    }
}
